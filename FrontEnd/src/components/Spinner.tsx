import { FallingLines } from 'react-loader-spinner';

const Spinner = () => {
    return <FallingLines
        color="#000000ff"
        width="25"
        visible={true}
    />;
};

export default Spinner;